en:
  graphql:
    mutations:
      desc: The schema’s entry-point for mutations.
      common:
        fields:
          id: ID
          uuid: UUID
      user:
        sign_up:
          desc: Sign up a user
        sign_out:
          desc: Sign out a user
          fields:
            completed: will return true if the user log out
        sign_in:
          desc: Sign in a user
        create_list:
          decs: Create list
        add_list_item:
          desc: Add list item
        remove_list_item:
          desc: Remove list item
        add_favorite_movie:
          decs: Add favorite movie
      auth:
        refresh_token:
          desc: Updating current access token with refresh token
